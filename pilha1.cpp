#include<stack>
#include<iostream>

using namespace std;

int main ()
{
	stack<int> pilha;
	
	pilha.push(10);
	pilha.push(9);
	pilha.push(8);
	pilha.push(7);
}
system('pause');
return 0;
